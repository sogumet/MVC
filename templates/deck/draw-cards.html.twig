{# test.html.twig #}

{% extends "base.html.twig"%}

{% block content %}

<div class="container-left">
    <h1>Draw some cards</h1>
    <div class="card" > 
        
        {% for card in data %}      
        <img src={{ card.image }}>
        {% else %}
        <p>Deck is emty</p>
        {% endfor %}
        
    </div>
    <p>{{ cardleft}} card left in deck</p>
    <ul>
    <li><a href="{{ url("deck")}}">Deck</a></li>
    <li><a href="{{ url("shuffle")}}">Shuffle</a></li>
    <li><a href="{{ url("draw")}}">Draw</a></li>
    </ul>
</div>


{% endblock %}