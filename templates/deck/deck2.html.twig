{# test.html.twig #}

{% extends "base.html.twig"%}
{% block title %}Card | Joker{% endblock %}
{% block content %}

<div class="container-left">
<div class="suit" >
    <h1>Deck</h1>
        {% for  i in 39..53 %}      
            <img src={{ asset(data[i].image) }}>
        {% endfor %}
        
</div>
<div class="suit">
        {% for  i in 26..38 %}      
            <img src={{ asset(data[i].image) }}>
        {% endfor %}
</div>
<div class="suit">
        {% for  i in 13..25 %}      
            <img src={{ asset(data[i].image) }}>
        {% endfor %}
</div>
<div class="suit">
        {% for  i in 0..12 %}      
            <img src={{ asset(data[i].image) }}>
        {% endfor %}
</div>
<ul>
    <li><a href="{{ url("shuffle")}}">Shuffle</a></li>
    <li><a href="{{ url("draw")}}">Draw</a></li>
    <li><a href="{{ url("deck")}}">Deck</a></li>
    <li><a href="{{ link_to_draw }}">Draw 4 cards</a></li>
    <li><a href="{{ link_to_deal }}">Poker deal</a></li>
</ul>
</div>
{% endblock %}