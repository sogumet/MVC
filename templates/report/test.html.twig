{# test.html.twig #}

{% extends "base.html.twig"%}

{% block content %}

<div>
    <h1>Your lucky number is {{ number }}</h1>
    {{ dump(arr) }}
    <img src={{ asset("img/deck/10-clubs.png")}} alt="">
    <p>bit shifted {{shift}} = {{bit}}</p>
    <p>100000000111100 =  {{dec}}</p>
    <p>{{dump(res)}}</p>
    {{dump(card)}}
    {{dump(card1)}}
    {{dump(hand1)}}


    <p>This is the hej {{ type }} lottery.</p>
    <div id="str"><p></p></div>
    <a href="{{ url("form-search")}}">Sök</a>
    <a href="{{ url("form-login")}}">Login</a>
    <a href="{{ url("form-session")}}">Session</a>
    <a href="{{ url("dice-home")}}">Dice 🂱</a>
    <a href="{{ url("dice-graphic-home")}}">Dice graphic</a>
    <a href="{{ url("dice-hand-home")}}">Dice hand</a>

</div>
<h1 class="ess">🂱</h1>
<h1 class="ess">🃄</h1>
'⚁'
'🂱'
'U+1F0B1'

{% endblock %}